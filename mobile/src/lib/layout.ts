/** Shared layout constants. TAB_BAR_HEIGHT is the tab bar's content height (excluding the bottom
 *  safe-area inset); screens add `insets.bottom` on top. Used by the tab navigator and by `Screen`'s
 *  `tabBarSpacing` so content clears the floating (frosted) tab bar. */
export const TAB_BAR_HEIGHT = 64;
