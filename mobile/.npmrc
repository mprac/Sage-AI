# React 19 + RN 0.81: several libraries (lucide-react-native, react-native-markdown-display,
# react-native-purchases) still declare React 16–18 as peers even though they work with 19.
# Default to legacy peer resolution so npm / `npx expo install` don't fail on stale peer metadata.
legacy-peer-deps=true
